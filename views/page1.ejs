<% include partials/header %>

    <h1><%= title %></h1>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>user</th>
                <th>count_user</th>
                <th>time_avg</th>
                <th>time_range</th>
            </tr>
        </thead>
        <tbody>
            <% table.forEach(function(d) { %>
            <tr>
            <td><%= d.user %></td>
            <td><%= d.count_user %></td>
            <td><%= d.time_avg %></td>
            <td><%= d.time_range %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>

<% include partials/footer %> 